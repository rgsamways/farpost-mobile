## 1. Backfill

- [x] 1.1 Document the app's existing color/shape/type-scale alignment with `docs/farpost-style-guide.md` as a real spec — no code change, already shipped in the M01 skeleton commit.
- [x] 1.2 Record the known, tracked gap (placeholder font instead of real Inter) as an explicit requirement rather than silently omitting it.
