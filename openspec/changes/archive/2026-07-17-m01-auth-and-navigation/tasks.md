## 1. Backfill

- [x] 1.1 Document the real sign-in → session-persistence → capability-routing → sign-out flow as a spec — no code change, already shipped in the M01 skeleton commit.
- [x] 1.2 Record the exact three-capability model (`ADMIN`/`PROFESSIONAL`/`OWNER`) as an explicit requirement, matching the backend.
